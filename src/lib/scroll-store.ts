/** Mutable store updated by the services section, read by the 3D model on each frame. */
export const scrollStore = {
  truckRotationY: Math.PI, // radians — negative = facing left (initial state)
};
